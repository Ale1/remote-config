This package is example of package that relies on Remote Config package being present to use (hard dependency).  

If using an NPM-style package registry, it will download the package as dependency the same was as UPM.  

See Purchase package for example setup of a package with Remote config as an optional dependency.
